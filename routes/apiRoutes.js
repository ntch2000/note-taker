// ALL API ROUTES HERE
const path = require("path");

module.exports = (app) => {
  const dbPath = "../db/db.json";

  // Populates the saved notes
  app.get("/api/notes", (req, res) => {
    // call read method
  });
};
